# OS / editor junk
Thumbs.db
ehthumbs.db
.DS_Store
desktop.ini
*.swp
.vscode/
.idea/

# local preview artifacts
_preview-qr.png

# Raw source photos dropped at the project root before processing.
# One of these has been checked and carries GPS EXIF data revealing where
# it was taken - never commit originals here, only the resized, metadata-
# stripped copies that Python writes into assets/images/.
hero-background.*
firsr-photo-heart.*
rest-of-the-grid.*
whatsapp-preview.*
